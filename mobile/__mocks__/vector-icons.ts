/* eslint-env jest */
// Minimal react-native-vector-icons mock — a string component stands in for every family.

const Icon = () => null;
Icon.displayName = 'VectorIcon';

module.exports = Icon;
module.exports.default = Icon;
